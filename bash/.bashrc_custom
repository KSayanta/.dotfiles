# .bashrc

# pnpm
export PNPM_HOME="/home/raj/.local/share/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME/bin:"*) ;;
  *) export PATH="$PNPM_HOME/bin:$PATH" ;;
esac

# gpg
GPG_TTY=$(tty)
export GPG_TTY

# yazi
y() {
  local tmp="$(mktemp -t "yazi-cwd.XXXXXX")" cwd
  yazi "$@" --cwd-file="$tmp"
  IFS= read -r -d '' cwd < "$tmp"
  [ -n "$cwd" ] && [ "$cwd" != "$PWD" ] && builtin cd -- "$cwd"
  rm -f -- "$tmp"
}

# Find dir
fd() {
  local dir
  dir=$(find . -type d ! -name .git ! -name node_modules | fzf +m) && cd "$dir"
}

# Open project dir
open() {
  local dir
  local dirList=""
  local paths=("Projects" "Public/projects")

  for path in "${paths[@]}"; do
    local currDir="$HOME/$path"
    dirList+=$(find "$currDir" -name .git -or -name node_modules -type d -print -prune)
    dirList+=$(echo " ")
  done

  dir=$(echo "$dirList"\
    | xargs du -sh\
    | sort -hr\
    | fzf +m --header "Select which one to open:"\
             --preview 'cat $(dirname {2})/package.json'\
             --preview-label 'package.json'\
             --preview-window '~3'\
    | awk '{print $2}'\
    | xargs dirname)

  cd "$dir"
}

# Scaffold a project
scaffold() {
  bunx sv create --template minimal \
    --types ts --add tailwindcss="plugins:typography" \
    --install bun $1
}

# Stow
stowth() {
  stow -vSt ~ $1
}

# Unstow
unstow() {
  stow -vDt ~ $1
}

### ARCHIVE EXTRACTION
# usage: ex <file>
ex () {
  if [ -f "$1" ] ; then
    case $1 in
      *.tar.bz2)   tar xjf $1   ;;
      *.tar.gz)    tar xzf $1   ;;
      *.bz2)       bunzip2 $1   ;;
      *.rar)       unrar x $1   ;;
      *.gz)        gunzip $1    ;;
      *.tar)       tar xf $1    ;;
      *.tbz2)      tar xjf $1   ;;
      *.tgz)       tar xzf $1   ;;
      *.zip)       unzip $1     ;;
      *.Z)         uncompress $1;;
      *.7z)        7z x $1      ;;
      *.deb)       ar x $1      ;;
      *.tar.xz)    tar xf $1    ;;
      *.tar.zst)   unzstd $1    ;;
      *)           echo "'$1' cannot be extracted via ex()" ;;
    esac
  else
    echo "'$1' is not a valid file"
  fi
}


### ALIASES

# Miscellaneous
alias reload='source ~/.zshrc'
alias h='history'
alias v="nvim"
alias neo="NVIM_APPNAME='nvim_test' nvim"
alias np="npm"
alias pn="pnpm"

# Git
alias gi="git init"
alias gid="git identity" # identity is git alias
alias gcl="git clone"
alias gclb="git clone --bare"
alias ga="git add"
alias gap="git add --patch"
alias gc="git commit"
alias gd="git diff --output-indicator-new=' ' --output-indicator-old=' '"
alias gf="git fetch --all"
alias gp="git push"
alias gu="git pull"
alias gui="gitui" # depends on gitui package
alias gl="git log --oneline --decorate=full -n 40"
alias gll="git log --oneline --abbrev-commit --date=relative --graph --pretty=format:'%C(auto)%h -%C(auto)%d %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'"
alias gb="git branch -vva"
alias gr="git remote -v"
alias gs="git status --short"
alias gsl="git status --long"
alias gwl="git worktree list"
alias gwa="git worktree add"
alias gwab="git worktree add -b"
alias gwr="git worktree remove"

# Transmission
alias tds="transmission-daemon --logfile ~/.local/log/transmission.log"
alias tde="transmission-remote --exit"
alias tre="transmission-remote"
alias trt="transmission-remote --torrent"

# Obsidian
alias gopen="gocryptfs ~/Database/ObsidianDBCrypt ~/Database/ObsidianDB/notes"
alias gclose="fusermount3 -u ~/Database/ObsidianDB/notes"

# Ordna
alias ord="node ~/bin/git_repos/ordna/packages/cli/dist/bin/ordna.js"
